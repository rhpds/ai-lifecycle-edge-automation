{{- if .Values.vmConnectivity.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: influxdb-exporter-vm
  namespace: microshift-001
spec:
  type: NodePort
  selector:
    kubevirt.io/domain: microshift
  ports:
  - port: 31081
    targetPort: 31081
    nodePort: 31081
    protocol: TCP
{{- end }}

